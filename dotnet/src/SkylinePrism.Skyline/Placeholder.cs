namespace SkylinePrism.Skyline;

/// <summary>
/// Placeholder so the project compiles before Layer 10 vendors the Skyline JSON-RPC
/// client (SkylineSession) and the report driver (SkylineReportDriver).
/// </summary>
internal static class Placeholder
{
}
